---
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  merge_group:

permissions: read-all

jobs:
  release:
    uses: "discrapp/.github/.github/workflows/release.yml@main"
    secrets: inherit
    permissions:
      contents: write
      issues: write
      pull-requests: write

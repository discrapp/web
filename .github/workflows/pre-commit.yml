---
on:
  pull_request:
  merge_group:
  push:
    branches:
      - main

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  pre-commit:
    uses: "discrapp/.github/.github/workflows/pre-commit.yml@main"
    secrets: inherit
    permissions:
      contents: read
      pull-requests: write
      issues: write
